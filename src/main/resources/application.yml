spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/myap
    username: postgres
    password: postgres
  jpa:
    hibernate:
      dll-auto: create
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation : true
    generate-ddl: true

server:
  port: 8090